struct PS_Input
{
    float2 UV : TEXCOORD0;
};

Texture2D input_texture : register(t0);
SamplerState tsampler : register(s0);

float4 main() : SV_Target
{
    return input_texture.Sample(tsampler, IN.UV);
}